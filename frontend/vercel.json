{
  "framework": null,
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "node scripts/git-info.mjs && npx vite build --base=/",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
