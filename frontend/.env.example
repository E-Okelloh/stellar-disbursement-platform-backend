# URL of the Stellar Disbursement Platform backend API.
# The backend now lives in its own repo:
# https://github.com/E-Okelloh/stellar-disbursement-platform-backend
# Clone and run it separately, then point this at wherever it's listening
# (e.g. http://localhost:8000 locally, or a deployed host/IP).
VITE_API_URL=http://localhost:8000
